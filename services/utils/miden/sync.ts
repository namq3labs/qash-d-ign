// Stubbed: @openzeppelin/miden-multisig-client and @miden-sdk/miden-sdk removed.

/**
 * Wrapper around multisig.syncAll() - stubbed since miden packages are not available.
 */
export async function resilientSyncAll(_multisig: any, _webClient: any): Promise<any> {
  throw new Error("resilientSyncAll not available: miden packages removed");
}
